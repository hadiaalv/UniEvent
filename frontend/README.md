# UniEvent (Next.js + Tailwind CSS)

This is a Next.js-based modern campus event management app. To run:

```bash
cd frontend
npm install
npm run dev
```

## Features
- Tailwind CSS styling (see /styles/globals.css)
- Modern UI (see pages/, components/)

*This replaces the original Vite/React template that was here.*
